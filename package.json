{
	"name": "atlasframework",
	"version": "0.0.1",
	"type": "module",
	"files": [
		"dist"
	],
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"import": "./dist/index.js"
		}
	},
	"scripts": {
		"check": "tsc --noEmit",
		"build": "npm run check && vite build && tsc",
		"dev": "vite build --watch"
	},
	"devDependencies": {
		"typescript": "^7.0.2",
		"vite": "^8.3.0"
	}
}
